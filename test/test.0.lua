function twice(x)
    y = (x * 2)
    return y
end

function square(a)
    return (a * a)
end

title_1 = "square area is"
a = 5
area = square(a)
title_2 = "and its perimeter is"
perimeter = twice(twice(a))
